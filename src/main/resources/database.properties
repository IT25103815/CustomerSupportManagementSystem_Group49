# Helpify uses Microsoft SQL Server through JDBC.
# Update these values for your local SQL Server instance, or set CSMS_DB_URL / CSMS_DB_USERNAME / CSMS_DB_PASSWORD environment variables.
db.url=jdbc:sqlserver://localhost:1433;databaseName=CustomerSupportDB;encrypt=true;trustServerCertificate=true
db.username=sa
db.password=CHANGE_ME
db.driver=com.microsoft.sqlserver.jdbc.SQLServerDriver
